---
status: draft
owner: product
deciders: [Lila Brooks]
---
# CLI feature name

## Purpose
Describe the job this CLI feature does for the user.

## Commands
List the command names, subcommands, flags, and arguments.

Example:

```bash
agent example run --name Lila
```

## Inputs
List arguments, flags, environment variables, files, and defaults.

## Outputs
Describe terminal output, files written, exit codes, and error messages.

## Behavior
Describe the rules the Python CLI must follow.

## Acceptance tests
List concrete checks the implementation must pass.

Example:

- Given `--name Lila`, output includes `Hello, Lila`.

